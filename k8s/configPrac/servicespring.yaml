apiVersion: v1
kind: Service
metadata:
  name: spring-service
  labels:
    app: spring-service
spec:
  type: NodePort
  selector:
    app: spring-demo
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31000
