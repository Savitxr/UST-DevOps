apiVersion: v1
kind: Service
metadata:
  name: monoservice
  labels:
    app: monoservice
spec:
  type: NodePort
  selector:
    app: monopod
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30004

