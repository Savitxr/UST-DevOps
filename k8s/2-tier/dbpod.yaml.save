apiVersion: v1
kind: Pod
metadata:
  name: db-pod
  labels:
    app: db-pod
spec:
  containers:
    - name: dbcont
      image: savitxr/2-tier-db:1
      ports:
        - containerPort: 3306
      env:
        secretRef:
          app: 2tier-secret
