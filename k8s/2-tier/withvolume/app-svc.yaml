apiVersion: v1
kind: Service
metadata:
  name: app-svc
  labels:
    app: app-svc
spec:
  type: NodePort
  selector:
    app: 2-tier-app-pod
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30007

